type: object
properties:
  apps:
    type: array
    items:
      $ref: ./App.yaml
    description: The list of apps.
  hasMore:
    type: boolean
    description: >-
      Flag indicating if there are more apps that are not present in the
      response.
  offset:
    type: integer
    description: The number of app records skipped in the returned list.

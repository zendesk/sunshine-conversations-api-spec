type: object
properties:
  integrations:
    type: array
    items:
      $ref: ./Integration.yaml
    description: The list of integrations.
  hasMore:
    type: boolean
    description: >-
      Flag indicating if there are more integrations that are not present in the
      response.
  offset:
    type: integer
    description: The number of integration records skipped in the returned list.

type: object
properties:
  serviceAccounts:
    type: array
    items:
      $ref: ./ServiceAccount.yaml
    description: The list of service accounts.
  hasMore:
    type: boolean
    description: >-
      Flag indicating if there are more service accounts that are not present in
      the response.
  offset:
    type: integer
    description: The number of service account records skipped in the returned list.

type: object
required:
  - channels
properties:
  channels:
    type: array
    description: An array of objects containing the channel type and the entity id.
    items:
      $ref: ./ChannelEntityItem.yaml

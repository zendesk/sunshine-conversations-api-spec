type: object
required:
  - integrationId
  - integrationType
properties:
  integrationId:
    type: string
    description: The ID of the target integration.
  integrationType:
    type: string
    description: >-
      The type of the target integration. See
      [**IntegrationTypeEnum**](Enums.md#IntegrationTypeEnum) for available
      values.

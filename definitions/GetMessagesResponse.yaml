type: object
properties:
  conversation:
    $ref: ./Conversation.yaml
  messages:
    type: array
    items:
      $ref: ./Message.yaml
  next:
    type: string
    description: The URI for the next set of messages in the conversation.

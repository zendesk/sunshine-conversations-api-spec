type: object
properties:
  displayName:
    allOf:
      - $ref: "../schemas/displayName.yaml"
    description: |
      A friendly name for the conversation, may be displayed to the business or
      the user.
    example: "My conversation"
  description:
    $ref: "../schemas/description.yaml"
  iconUrl:
    $ref: "../schemas/icon.yaml"
  metadata:
    $ref: "../schemas/metadata.yaml"

type: object
properties:
  target:
    $ref: "../schemas/webhooks/target.yaml"
  triggers:
    $ref: "../schemas/webhooks/triggers.yaml"
  includeFullUser:
    $ref: "../schemas/webhooks/includeFullUser.yaml"
  includeFullSource:
    $ref: "../schemas/webhooks/includeFullSource.yaml"

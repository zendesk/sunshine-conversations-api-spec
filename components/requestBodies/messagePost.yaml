type: object
properties:
  author:
    allOf:
      - $ref: "../schemas/author.yaml"
    description: The author of the message.
  content:
    allOf:
      - $ref: "../schemas/content.yaml"
    description: The content of the message.
  destination:
    $ref: "../schemas/destination.yaml"
  metadata:
    $ref: "../schemas/metadata.yaml"
  override:
    $ref: "../schemas/messageOverride.yaml"
required:
  - author
  - content

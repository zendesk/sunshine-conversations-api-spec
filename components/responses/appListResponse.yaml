type: object
properties:
  apps:
    type: array
    description: List of returned apps.
    items:
      $ref: "../schemas/app.yaml"
  meta:
    $ref: "../schemas/meta.yaml"
  links:
    $ref: "../schemas/links.yaml"

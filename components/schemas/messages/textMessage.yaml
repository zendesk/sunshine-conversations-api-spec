type: object
properties:
  type:
    type: string
    description: The type of message.
    default: "text"
  text:
    type: string
    description: The text content of the message. Optional only if actions are provided.
    maxLength: 4096
    example: "Hello!"
  actions:
    type: array
    description: Array of message actions.
    items:
      $ref: "action.yaml"
required:
  - type

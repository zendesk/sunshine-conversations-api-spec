allOf:
  - $ref: "matchCriteriaBase.yaml"
  - type: object
    description:
      The set of criteria used to determine the user's identity on a third-party
      channel.
    properties:
      type:
        type: string
        description: The channel type.
        default: "mailgun"
      address:
        type: string
        description: The user’s email address.
        example: "steveb@channel5.com"
      subject:
        type: string
        description: May be specified to set the subject for the outgoing email.
        default: "New message from {appName}"
    required: 
      - address